//by LOVELY SHARMA
//program to take a character input in lower case converting it into upper case

#include <stdio.h>
int main(void) {
        char la;
        printf("\n enter any character in lower case: ");
	scanf("%c",&la);
	printf("\n The upper character is %c ",la-32);
	return 0;
	}
